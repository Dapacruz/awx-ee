---
version: 3
images:
  base_image:
    name: quay.io/centos/centos:stream9
dependencies:
  python_interpreter:
    package_system: python3.11
    python_path: /usr/bin/python3.11
  ansible_core:
    package_pip: ansible-core>=2.18.2,<2.19
  ansible_runner:
    package_pip: ansible-runner
  galaxy: |
    ---
    collections:
      - name: ansible.posix
      - name: ansible.windows
      - name: awx.awx
  system: |
    cmake [platform:rpm compile]
    epel-release [platform:rpm]
    gcc [platform:rpm compile]
    gcc-c++ [platform:rpm compile]
    git-core [platform:rpm]
    git-lfs [platform:rpm]
    krb5-devel [platform:rpm compile]
    krb5-workstation [platform:rpm]
    libcurl-devel [platform:rpm compile]
    make [platform:rpm compile]
    openssl-devel [platform:rpm compile]
    podman-remote [platform:rpm]
    python3.11-devel [platform:rpm compile]
    rsync [platform:rpm]
    sshpass [platform:rpm]
    subversion [platform:dpkg]
    subversion [platform:rpm]
    unzip [platform:rpm]
  python: |
    git+https://github.com/ansible/ansible-sign
    ncclient
    paramiko
    pexpect>=4.5
    pyOpenSSL
    pykerberos
    pypsrp[kerberos,credssp]
    python-daemon
    pywinrm[kerberos,credssp]
    pyyaml
    receptorctl
    six
    toml
additional_build_steps:
  append_base:
    - RUN $PYCMD -m pip install -U pip
  append_final:
    - COPY --from=quay.io/ansible/receptor:devel /usr/bin/receptor /usr/bin/receptor
    - RUN mkdir -p /var/run/receptor
    # - RUN mkdir -p /.ansible/tmp && chmod -R 770 /.ansible
    # - RUN mkdir -p /.cache/pip && chmod -R 770 /.cache
    - RUN git lfs install --system
    # SymLink `python` -> `python3.11`
    - RUN alternatives --install /usr/bin/python python /usr/bin/python3.11 1
    # SymLink `python3` -> `python3.11`
    - RUN alternatives --install /usr/bin/python3 python3 /usr/bin/python3.11 1
